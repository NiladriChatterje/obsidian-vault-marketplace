# Copy to .env and fill in. Shared by the Expo app, the Next.js site (web/) and the Fastify payment server (server/).
# Leave the Supabase keys empty to run app + site in demo mode (sample vaults, fake auth, local purchases).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Platform commission on paid sales, in percent. Must match PLATFORM_FEE_PERCENT read by the server.
# The commission's percentage half, split by where the SELLER banks. A domestic payout is a
# near-free local transfer with nothing converted; reaching a seller abroad costs a transfer
# fee and a conversion spread, so that sale carries the higher rate instead of being
# subsidised by domestic ones. Both must stay above NEXT_PUBLIC_PROVIDER_PERCENT_FEE.
NEXT_PUBLIC_PLATFORM_COUNTRY=IN
NEXT_PUBLIC_PLATFORM_FEE_PERCENT_DOMESTIC=8
NEXT_PUBLIC_PLATFORM_FEE_PERCENT_INTERNATIONAL=12
# The fixed half of the commission, in minor units. It exists to cancel the provider's own
# flat fee: a percentage-only commission always loses money below some price, because the
# percentage shrinks with the price and the provider's flat charge does not.
NEXT_PUBLIC_PLATFORM_FEE_FIXED_CENTS=4000
# What the provider takes. Dodo is 4% + $0.40; the fixed part is in the listing currency
# with a little headroom over the exchange rate. Keep the commission's two halves at or
# above these or the platform loses money on a sale.
NEXT_PUBLIC_PROVIDER_PERCENT_FEE=4
NEXT_PUBLIC_PROVIDER_FIXED_FEE_CENTS=3500
# Cheapest paid listing. A product choice, not a solvency one: the commission is profitable
# at any price, but at a very low one the flat part is most of the sale.
MIN_PRICE_CENTS=4900

# Payment server (server/). The app and site call it for orders and payout onboarding.
SERVER_API_URL=http://localhost:4000
# Same URL for the Expo app: Metro inlines only EXPO_PUBLIC_* names into the bundle.
EXPO_PUBLIC_API_URL=http://localhost:4000
# Port for server/. Hosts that inject PORT (Render, Fly, Railway) are used when this is unset.
API_PORT=4000
# Keep-warm ping for free tiers that idle a service out. Target defaults to RENDER_EXTERNAL_URL,
# then SERVER_API_URL; skipped automatically when that is localhost. Off on paid instances.
# KEEP_AWAKE=off
# KEEP_AWAKE_MINUTES=14
# KEEP_AWAKE_URL=
# Public origin of the website. Optional in the browser (defaults to location.origin); required for app buyers.
# NEXT_PUBLIC_REDIRECT_ORIGIN=https://vault.market
# Comma-separated origins allowed as post-checkout redirect targets. Leave empty to allow any http(s) origin.
# ALLOWED_REDIRECT_ORIGINS=https://vault.market,http://localhost:3000

# Server-only Supabase key: lets the payment server record purchases and resolve MCP tokens.
# Never prefix with EXPO_PUBLIC_ or NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
# Without the two keys above the server runs in demo mode, which trusts an x-demo-user header
# and grants every caller ownership of every vault. A hosted instance refuses to start in that
# state; set this to yes only for a deliberately public demo with no real listings.
# ALLOW_PUBLIC_DEMO=yes

# Where a sign-in challenge waits between the password and the emailed code. Required: with
# no Redis nobody can finish signing in, which is the right way to fail, since the alternative
# is letting a password through on its own. Any redis:// or rediss:// URL.
REDIS_URL=

# The vault scanner (antivirus/), which every uploaded zip is posted to before it is unpacked
# into the store. A listing is downloaded by strangers, so what a seller sends is scanned rather
# than trusted for being named .zip. docker-compose builds it as the `antivirus` service and
# sets this for the api container; set it here only when the scanner lives somewhere else, such
# as a Render private service (the scheme may be left off). Left empty nothing is scanned, which
# is for laptops. When it is set, a scanner that cannot be reached refuses the upload (503)
# rather than letting it through. See antivirus/README.md.
SCANNER_URL=
# Shared secret, if the scanner was deployed with one. Must match its own SCANNER_TOKEN.
SCANNER_TOKEN=

# Queued uploads (worker/). With these set, the browser puts the zip straight into an
# S3-compatible store (MinIO in docker-compose), the api writes a ticket to Redis and answers at
# once, and the worker scans and unpacks it in turn; a rush of uploads waits in line instead of
# timing out against the scanner. With any of the first four missing, POST /uploads/vault-zip does
# the whole job inline as before. docker-compose sets all of them for the api and worker
# containers, so on a laptop leave them empty.
QUEUE_REDIS_URL=
MINIO_ENDPOINT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
# The store as the BROWSER reaches it; presigned upload links carry this host. Defaults to
# MINIO_ENDPOINT, or under compose to http://localhost:9000. Set it when the site is opened
# from anywhere but the machine running compose.
MINIO_PUBLIC_URL=
# MINIO_BUCKET=vault-uploads
# Zips one worker handles at once; each holds up to 70 MB and one scanner thread.
# WORKER_CONCURRENCY=2
# Credentials of the MinIO container itself (compose hands them to the api and worker as the two
# keys above). The password must be at least 8 characters; change it anywhere but a laptop.
MINIO_ROOT_USER=vaultmarket
MINIO_ROOT_PASSWORD=vaultmarket-dev-secret

# Brevo transactional email (app.brevo.com -> SMTP & API -> API Keys). Leave empty and the
# server sends no mail. BREVO_EMAIL_FROM must be a sender Brevo has verified. Sign-in codes
# go out this way too, so check Security -> Authorised IPs allows wherever the server runs.
BREVO_API_KEY=
BREVO_EMAIL_FROM=obsidian.vault.marketplace@gmail.com
BREVO_EMAIL_FROM_NAME=Vault Market

# The vault store (server/src/vault-store.ts): where every listing's note bodies, attachments,
# cover and scanned zip live for good. Any S3-compatible bucket; docker-compose runs a second
# MinIO (minio-store) and sets these for the api and worker. The listings themselves, the note
# index and storage plans are tables in Supabase: apply supabase/migrations/0018 once.
# Without an endpoint the catalog routes answer 501. Keys fall back to MINIO_ACCESS_KEY /
# MINIO_SECRET_KEY when left empty, for a single-MinIO setup with two buckets.
VAULT_STORE_ENDPOINT=
VAULT_STORE_ACCESS_KEY=
VAULT_STORE_SECRET_KEY=
# VAULT_STORE_BUCKET=vault-store
# Credentials of the minio-store container itself (docker-compose hands them to the api and
# worker as the two keys above). At least 8 characters; change it anywhere but a laptop.
VAULT_STORE_ROOT_USER=vaultstore
VAULT_STORE_ROOT_PASSWORD=vaultstore-dev-secret

# Storage plans (server/src/plans.ts): what a seller may keep listed across all their vaults,
# measured unpacked. No row in seller_plans means free. Operators grant a plan with
# POST /admin/sellers/:userId/plan until subscriptions are sold.
# PLAN_FREE_MB=600
# PLAN_PLUS_MB=2048
# PLAN_PRO_MB=5120

# server = listings come from the payment server's catalog (default when SERVER_API_URL is set); local = built-in demo data.
NEXT_PUBLIC_CATALOG_SOURCE=server

# --- Dodo Payments (merchant of record; the global rail) ---------------------
# Dodo is the legal seller to the buyer: it takes payment worldwide, remits VAT and
# sales tax, and settles the net to your bank. Dashboard -> Developer -> API Keys.
DODO_API_KEY=
# Dashboard -> Developer -> Webhooks. Point it at https://<api-domain>/webhooks/dodo and
# subscribe to payment.succeeded, payment.failed, refund.succeeded, dispute.lost and
# payout.success. The last is how a sale becomes payable to its seller: it says Dodo has
# paid us for it. Missed ones are caught up daily from Dodo's payout list.
DODO_WEBHOOK_SECRET=
# 'live' uses live.dodopayments.com; anything else stays on the test host.
DODO_ENVIRONMENT=test

# Supabase user ids allowed to reach /admin/* on the server: the seller payout ledger and the
# dashboard behind the site's Admin tab (every purchase, each vault's buyers and reviews).
# Comma-separated. Unset closes those routes entirely rather than leaving them open.
ADMIN_USER_IDS=

# Shortly after boot and then every PAYOUT_WATCH_HOURS (24), the server prepares a payout
# run for every seller whose balance has cleared and is worth sending, and mails you the
# list so nobody has to remember to open /admin/payouts. The mail needs BREVO_API_KEY and
# goes once per cadence however often the process restarts; runs are prepared regardless.
# Falls back to the Brevo sender address. PAYOUT_WATCH=off disables the whole check.
PAYOUT_WATCH_EMAIL=

# --- Paying sellers ------------------------------------------------------------
# Sellers are paid once a month, on PAYOUT_CYCLE_DAY (1-28) reckoned in PAYOUT_CYCLE_TIMEZONE,
# and only once their cleared balance has reached the threshold for where the money goes:
# PAYOUT_THRESHOLD_DOMESTIC_CENTS inside PLATFORM_COUNTRY, PAYOUT_THRESHOLD_INTERNATIONAL_CENTS
# anywhere else. Both are in the platform currency's minor units; the international one is
# meant to be about USD 70, so revisit it when the exchange rate moves a long way.
# A transfer fee is also a fixed cost per payout, so on top of the floor a balance is held
# until the sales behind it have earned enough commission to cover sending it; that part is
# derived from the transfer costs below, per seller, from how they asked to be paid.
PAYOUT_CYCLE_DAY=28
PAYOUT_CYCLE_TIMEZONE=Asia/Kolkata
PAYOUT_THRESHOLD_DOMESTIC_CENTS=500000
PAYOUT_THRESHOLD_INTERNATIONAL_CENTS=620000
PLATFORM_COUNTRY=IN
# What listings are priced in and balances are held in. A seller paid in another currency
# is sent this amount converted at transfer time; a payout run carries both currencies.
PLATFORM_CURRENCY=INR
TRANSFER_COST_BANK_DOMESTIC=500
TRANSFER_COST_BANK_INTERNATIONAL=150000
TRANSFER_COST_WISE=20000
TRANSFER_COST_PAYONEER=20000
TRANSFER_COST_PAYPAL=15000
# Headroom over the derived break-even, so a payout is never marginal.
PAYOUT_SAFETY_FACTOR=1.35
# Days a sale is held before it can be paid out, so a reversed payment is never one we have
# already sent on. The longer window covers the statutory right of withdrawal on digital
# goods in the EU, EEA and UK; elsewhere a short hold against ordinary reversals is enough.
# An unknown buyer country gets the longer one.
HOLDBACK_DAYS_WITHDRAWAL=14
HOLDBACK_DAYS_DEFAULT=3
# On top of the window, a sale is payable only once Dodo has actually settled it to us, so
# a seller is never paid from the platform's pocket while the money sits at Dodo. 'no' uses
# the time window alone: only for test mode, where Dodo never pays out.
PAYOUT_REQUIRE_SETTLEMENT=yes

# Signs the short-lived download links buyers follow. Falls back to DODO_API_KEY, so setting
# it is optional; set it if you ever rotate the Dodo key without wanting live links to break.
DOWNLOAD_SECRET=
